const express = require("express")

const BookingControl = require("../controllers/booking-controller") // MongoDb functions

const router = express.Router()

// Need to create router commands from controller